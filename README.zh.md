# Light Stats

一款 macOS 菜单栏应用，实时监控 CPU、GPU、内存、磁盘和网络使用情况。点击任意项目可在浮动面板中查看详情，支持内存清理。

> 英文版 README 请查看 [README.md](README.md)

---

## 功能简介

Light Stats 常驻菜单栏，实时显示系统指标，无需额外窗口。

**设计原则：**

- 信息常驻菜单栏，随时可见
- 固定宽度布局，避免数字变化导致界面跳动
- 专注监控，清理为辅
- 协议抽象 + 依赖注入，便于测试和扩展

---

## 功能详情

### 菜单栏状态栏

两行布局：

- 上行：数值（`62%`、`99 GB`），11pt 等宽字体
- 下行：标签（`CPU`、`DISK`），8pt，浅色
- 网络项：上行上传速度 ↑，下行下载速度 ↓，10pt 等宽字体

固定宽度防止抖动（CPU 26pt，Disk 46pt）。可在设置中开关各项显示（Logo、CPU、GPU、MEM、Disk、Net、Fan）。

### 浮动面板

点击菜单栏项目打开 NSPanel 浮动面板（360×520），包含两个标签页：

**概览：** CPU、GPU、内存数值卡片，负载均值，温度 / 风扇 / 磁盘状态条，网络速度，CPU 进程排行，P/E 核心使用率柱状图。

**清理：** 内存压力概览与交换空间预警，App 列表按内存占用降序排列，支持正常关闭和强制退出（二次确认），可展开查看子进程。

### 设置

- 菜单栏显示项目开关
- 刷新频率：低 (5s)、中 (2s)、高 (1s)
- 单位：温度 (℃/℉)、网速 (自动/KB/s/MB/s)
- 语言：简体中文、English、日本語、跟随系统

---

## 技术栈

- Swift 5.9+
- SwiftUI（面板/设置）+ AppKit（菜单栏/绘制）
- Combine + Swift Concurrency
- macOS 14+
- Mach API、IOKit、SMC、getifaddrs 获取系统指标

### 架构设计

每个监控维度有独立的读取器和管理类。通过 `ProcessService`、`SettingsManaging` 等协议解耦实现。依赖注入通过构造函数或属性注入，便于测试。全局常量集中在 `AppConfig` 管理。

---

## 实现状态 (Updated: 2026-01-16)

### 菜单栏状态栏

| 功能 | 状态 |
|------|------|
| NSStatusItem 框架 | ✅ 已完成 |
| 双行布局 | ✅ 已完成 |
| 固定宽度 | ✅ 已完成 |
| 显示项开关 | ✅ 已完成 |
| 最少显示校验 | ✅ 已完成 |
| 风扇动画 | ❌ 未实现 |

### 浮动面板

| 功能 | 状态 |
|------|------|
| 双标签页 | ✅ 已完成 |
| 数值卡片 | ✅ 已完成 |
| 核心使用率柱状图 | ✅ 已完成 |
| 进程排序 | ✅ 已完成 |
| 强制退出 | ✅ 已完成 |
| 自动关闭 | ✅ 已完成 |

### 全局特性

| 功能 | 状态 |
|------|------|
| 多语言 | ✅ 已完成 |
| 依赖注入 | ✅ 已完成 |
| 全局配置 | ✅ 已完成 |
| 温度单位 | ⚠️ 可配置但视图未接入 |
| 网速单位 | ⚠️ 可配置但视图未接入 |

---

## 文件结构

- `Models/`: 基础数据结构（CPUInfo, DiskInfo 等）
- `Services/`: 系统级数据采集（ProcessService, SMCInfo）
- `ViewModels/`: 业务逻辑与状态管理（SystemMonitor, AppMemoryManager, SettingsManager）
- `Views/`:
  - `StatusBar/`: 菜单栏绘图逻辑
  - `Popover/`: 面板组件与标签页视图
  - `Settings/`: 设置页面 UI
  - `About/`: 自定义关于窗口
- `Resources/`: 国际化资源文件（`Localizable.strings`）
- `Utilities/`: 格式化工具与通用辅助方法

---

## 后续计划

Light Stats 从原型逐步演进为完整的系统监控工具。基础功能已就绪：协议抽象、依赖注入、多语言支持。

待办事项：

- 温度与网速单位设置接入视图显示
- 风扇动画优化
- 更多监控指标

---

> For English version, see [README.md](README.md)
